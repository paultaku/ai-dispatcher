# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

dev-scheduler is a Python 3.13+ project. The codebase is in early development with minimal structure.

## Development Setup

This project uses Python 3.13 with a simple structure managed via pyproject.toml.

### Running the Application

```bash
python main.py
```

## Project Structure

Currently a single-file application with `main.py` as the entry point.
